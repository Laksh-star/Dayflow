DAYFLOW_BACKEND_URL =
DAYFLOW_DEVELOPMENT_TEAM =
DAYFLOW_CODE_SIGN_IDENTITY = -
DAYFLOW_CODE_SIGN_STYLE = Manual

#include? "LocalSecrets.xcconfig"

DEVELOPMENT_TEAM = $(DAYFLOW_DEVELOPMENT_TEAM)
CODE_SIGN_IDENTITY = $(DAYFLOW_CODE_SIGN_IDENTITY)
CODE_SIGN_IDENTITY[sdk=macosx*] = $(DAYFLOW_CODE_SIGN_IDENTITY)
CODE_SIGN_STYLE = $(DAYFLOW_CODE_SIGN_STYLE)
